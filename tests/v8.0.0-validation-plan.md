# v8.0.0 Validation Plan

Validates the v8.0.0 module refactor (`feature/automated-testing` branch) on a Windows machine.

**Branch:** `feature/automated-testing`
**What changed:** Monolithic `LsiGitCheckout.ps1` (2746 lines, PS 5.1) refactored into module architecture (PS 7.6 LTS) with automated Pester tests. See `CHANGELOG.md` for full details.

**Date:** \_\_\_\_\_\_\_\_\_\_\_\_
**Tester:** \_\_\_\_\_\_\_\_\_\_\_\_
**Machine:** \_\_\_\_\_\_\_\_\_\_\_\_

---

## Phase 0: Environment Setup

```powershell
# 0.1 — PowerShell 7.6 LTS (install if missing: winget install Microsoft.PowerShell)
pwsh --version

# 0.2 — Git
git --version

# 0.3 — Pester 5.x (install if missing)
pwsh -Command "Get-Module Pester -ListAvailable | Select-Object Name, Version"
# If missing:
# pwsh -Command "Install-Module Pester -Force -MinimumVersion 5.0 -Scope CurrentUser"

# 0.4 — Clone and checkout the branch
git clone <repo-url> LsiGitCheckout
cd LsiGitCheckout
git checkout feature/automated-testing
```

| Step | Expected | Actual | Pass? |
|------|----------|--------|-------|
| 0.1 pwsh version | 7.6.x | | |
| 0.2 git version | any | | |
| 0.3 Pester version | 5.x+ | | |
| 0.4 Branch checkout | On feature/automated-testing | | |

---

## Phase 1: Module Loading

These verify that the refactored module structure loads correctly.

```powershell
# 1.1 — Module imports without errors
pwsh -Command "Import-Module ./LsiGitCheckout.psm1 -Force -Verbose"

# 1.2 — Manifest is valid
pwsh -Command "Test-ModuleManifest ./LsiGitCheckout.psd1"

# 1.3 — All 36 functions are exported
pwsh -Command "Import-Module ./LsiGitCheckout.psm1 -Force; (Get-Command -Module LsiGitCheckout).Count"

# 1.4 — Initialize function accepts parameters without error
pwsh -Command "Import-Module ./LsiGitCheckout.psm1 -Force; Initialize-LsiGitCheckout -ScriptPath ."
```

| Step | Expected | Actual | Pass? |
|------|----------|--------|-------|
| 1.1 Import module | No errors, verbose output shows functions loading | | |
| 1.2 Test manifest | Valid manifest with version 8.0.0 | | |
| 1.3 Function count | 36 | | |
| 1.4 Initialize | No errors | | |

---

## Phase 2: Unit Tests

These test pure/near-pure functions with no network or git operations. Verifies that the function extraction into the `.psm1` module preserved correctness.

```powershell
pwsh -Command "Invoke-Pester ./tests/LsiGitCheckout.Unit.Tests.ps1 -Output Detailed"
```

**Functions tested:** `Parse-VersionPattern`, `Test-SemVerCompatibility`, `Get-CompatibleVersionsForPattern`, `Select-VersionFromIntersection`, `Get-SemVersionIntersection`, `Format-SemVersion`, `Get-TagIntersection`, `Get-HostnameFromUrl`, `Validate-DependencyConfiguration`, `Get-AbsoluteBasePath`

| Result | Value |
|--------|-------|
| Tests Passed | |
| Tests Failed | |
| Tests Skipped | |
| Pass? | |

If any fail, record `Describe` block and `It` name:

```text

```

---

## Phase 3: Integration Tests (DryRun)

Runs the refactored entry point (`LsiGitCheckout.ps1`) with `-DryRun` against all 16 test configs. Verifies the entry point correctly imports the module and delegates to module functions. **Requires network access to GitHub.**

```powershell
pwsh -Command "Invoke-Pester ./tests/LsiGitCheckout.Integration.Tests.ps1 -Output Detailed"
```

| # | Config | Mode | Expected | Actual | Pass? |
|---|--------|------|----------|--------|-------|
| 1 | `dependencies_semver.json` | SemVer | 0 | | |
| 2 | `dependencies_semver-floating-versions.json` | SemVer | 0 | | |
| 3 | `dependencies_semver-floating-versions-2.json` | SemVer | 0 | | |
| 4 | `dependencies_semver.custom-dependency-path-1.json` | SemVer | 0 | | |
| 5 | `dependencies_semver.custom-dependency-path-2.json` | SemVer | 0 | | |
| 6 | `dependencies_semver.post-checkout-scripts.json` | SemVer | 0 | | |
| 7 | `dependencies_semver.post-checkout-scripts-2.json` | SemVer | 0 | | |
| 8 | `dependencies_semver.post-checkout-scripts-depth-0.json` | SemVer | 0 | | |
| 9 | `dependencies.recursive.example.json` | Agnostic | 0 | | |
| 10 | `dependencies.custom-dependency-path.json` | Agnostic | 0 | | |
| 11 | `dependencies.partial-API-overlap.json` | Agnostic | 0 | | |
| 12 | `dependencies.post-checkout-scripts.json` | Agnostic | 0 | | |
| 13 | `dependencies.post-checkout-scripts-2.json` | Agnostic | 0 | | |
| 14 | `dependencies.post-checkout-scripts-depth-0.json` | Agnostic | 0 | | |
| 15 | `dependencies.API-incompatibility-test.json` | Agnostic | 1 | | |
| 16 | `dependencies_semver.API-incompatibility-test.json` | SemVer | 1 | | |

---

## Phase 4: VS Code Workspace

Validates that the enriched workspace file provides a working developer experience.

1. Open workspace: `code LsiGitCheckout.code-workspace`
1. VS Code should recommend installing the PowerShell extension (if not already installed)
1. Open integrated terminal — should default to `pwsh`, not Windows PowerShell 5.1
1. Run and Debug sidebar (Ctrl+Shift+D) > select **"Run Unit Tests"** > press F5 > tests execute
1. Set a breakpoint in `LsiGitCheckout.psm1` (e.g., first line of `Parse-VersionPattern`), run **"Run Unit Tests"** > debugger pauses at breakpoint
1. Run and Debug > select **"Run Script (DryRun - SemVer)"** > F5 > script runs with DRY RUN output

| Step | Expected | Actual | Pass? |
|------|----------|--------|-------|
| 4.1 Workspace opens | Project loads | | |
| 4.2 Extension prompt | PowerShell extension recommended | | |
| 4.3 Terminal is pwsh | `$PSVersionTable.PSVersion` = 7.6.x | | |
| 4.4 Debug unit tests | Tests run in debug console | | |
| 4.5 Breakpoint works | Debugger pauses at breakpoint | | |
| 4.6 DryRun debug | Script runs, DRY RUN output visible | | |

---

## Phase 5: Smoke Test (Real Checkout)

Runs a real checkout (not DryRun) to verify end-to-end behavior with actual git operations. This confirms the module refactor didn't break the core functionality.

```powershell
# 5.1 — Real checkout
pwsh -File ./LsiGitCheckout.ps1 -InputFile tests/dependencies_semver.json -DisablePostCheckoutScripts

# 5.2 — Check exit code
echo $LASTEXITCODE

# 5.3 — Idempotent re-run (repos already at correct tag)
pwsh -File ./LsiGitCheckout.ps1 -InputFile tests/dependencies_semver.json -DisablePostCheckoutScripts

# 5.4 — Compare behavior with v7.1.0 (optional, if old script is available)
# Run the same config with the old monolithic script and diff the output
```

| Step | Expected | Actual | Pass? |
|------|----------|--------|-------|
| 5.1 Real checkout | Repos cloned/checked out successfully | | |
| 5.2 Exit code | 0 | | |
| 5.3 Idempotent re-run | Already up-to-date, exit 0 | | |
| 5.4 Matches v7.1.0 (optional) | Same repos at same tags | | |

---

## Summary

| Phase | Description | Result |
|-------|-------------|--------|
| 0 | Environment setup | |
| 1 | Module loading | |
| 2 | Unit tests (10 function groups) | |
| 3 | Integration tests (16 configs, DryRun) | |
| 4 | VS Code workspace + debugging | |
| 5 | Smoke test (real checkout) | |

### Overall Result

PASS / FAIL

### Issues Found

```text

```

### Additional Notes

```text

```
